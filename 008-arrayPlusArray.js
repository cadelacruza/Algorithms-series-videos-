function arrayPlusArray(arr1, arr2) {
    const sumOne = arra1.reduce((accumulator, current) => accumulator + current);
    const sumTwo = arra2.reduce((accumulator, current) => accumulator + current);

    return sumOne + sumTwo;
  }


  //[1,2,3] 
  // 1
  // 2